# sd-webui-prompt-translator
Prompt-aware multilingual translation backend for Stable Diffusion workflows. Preserves LoRA tags, weights, scheduled prompts, semantic blocks, regex patterns, and parser syntax while translating only valid text spans using a tokenizer/parser adapter architecture.
